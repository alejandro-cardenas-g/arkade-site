# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Next.js 16 Breaking Changes

This project uses Next.js 16.3.4, which has significant breaking changes from earlier versions. Before writing any code:
- Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this directory)
- Check AGENTS.md for additional context
- Heed all deprecation notices

## Build and Development

- `npm run dev` - Start the development server
- `npm run build` - Create a production build
- `npm run start` - Run the production server
- `npm run lint` - Run ESLint checks

## Code Style

- TypeScript strict mode is enabled and required
- ESLint configuration: Next.js core-web-vitals + TypeScript plugins
- Path alias `@/*` maps to the project root
- No code formatter (Prettier/Biome) is enforced; follow ESLint rules

## Project Structure

- App directory structure (Next.js 13+ style)
- Layout and page files use `.tsx` extension
- React 19.2.8 with Tailwind CSS 4

## Development Methodology

This project uses Spec-Driven Design. External skills are available:
- `spec` - Specification skill
- `spec-impl` - Implementation skill

Reference these when designing features or implementing from specifications.

## Important Notes

- AGENTS.md is auto-generated by `next dev` (from `node_modules/next/dist/server/lib/generate-agent-files.js`). Do not commit changes to it — they will be re-added on the next dev run.
- No testing framework is currently configured
- No CI/CD or deployment workflows are set up yet
